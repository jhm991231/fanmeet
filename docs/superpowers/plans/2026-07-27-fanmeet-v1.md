# fanmeet v1 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans로 태스크 단위 실행.
> **이 계획의 특수 규칙(사용자 합의)**: 코드는 Claude가 작성하되, 태스크당 작은 조각으로
> 나눠 설명과 함께 진행한다. 각 태스크 완료 시 사용자가 이해했는지 확인 후 다음으로
> 넘어간다. 코드 전문을 계획서에 싣지 않는 것은 이 합의에 따른 의도적 결정이다.
> 계획서는 목표·파일·인터페이스·테스트 케이스·완료 기준을 고정하는 계약서 역할을 한다.

**Goal:** 스트리머 팬미팅/시참 슬롯 예약 서비스 v1 — 소셜 로그인(OAuth2), JWT 인증,
SMS 휴대폰 인증·알림, PortOne 예약금 결제·환불까지 동작하는 웹 서비스.

**Architecture:** Spring Boot 모놀리스 + React(Vite) 프론트. 외부 연동(PortOne, CoolSMS)은
`PaymentGateway`/`SmsSender` 인터페이스 뒤에 격리. 정원 검증은 비관적 락 + COUNT.

**Tech Stack:** Java 21, Spring Boot 4.1(3.x OSS 종료로 착수 시 변경), Spring Security(OAuth2 Client), JPA+MySQL 8,
jjwt, PortOne 서버 SDK, CoolSMS(Nurigo), springdoc, React+Vite, Docker Compose.

**설계 문서:** `docs/superpowers/specs/2026-07-27-fanmeet-reservation-design.md`

## Global Constraints

- 금액은 정수(원화). Reservation.amount는 예약 시점 스냅샷.
- TimeSlot에 예약 카운터 컬럼 금지 — 락 + COUNT로 검증.
- Payment는 Reservation에 N:1 (결제 시도 로그). 성공 결제는 예약당 최대 1건(서비스 로직).
- 프론트가 보낸 결제 금액은 절대 신뢰하지 않는다 — 서버가 PortOne 조회로 대조.
- SMS 실패는 핵심 트랜잭션을 막지 않는다.
- 외부 API 호출부는 반드시 인터페이스(`PaymentGateway`, `SmsSender`) 뒤에.
- 시크릿(클라이언트 키, JWT 시크릿 등)은 환경변수/.env — 커밋 금지.
- Reservation.status: `PENDING_PAYMENT → CONFIRMED → CANCELLED`, `PENDING_PAYMENT → EXPIRED`.
- 브랜치: main에 직접 커밋(1인 학습 프로젝트), 태스크당 1개 이상 커밋.

## 사용자 준비물 (각 태스크 시작 전까지)

- [ ] A. 카카오 개발자 앱 등록 (Task 4 전) — Redirect URI: `http://localhost:8080/login/oauth2/code/kakao`
- [ ] B. 구글 클라우드 OAuth 클라이언트 (Task 7 전) — Redirect URI: `http://localhost:8080/login/oauth2/code/google`
- [ ] C. CoolSMS 계정 + 발신번호 등록 + 소액 충전 (Task 9 전)
- [ ] D. PortOne 계정 + 테스트 채널 + 웹훅은 로컬 테스트용 터널 (Task 14 전)

---

### Task 1: 백엔드 스캐폴딩 + 로컬 인프라

**Files:**
- Create: `backend/build.gradle`, `backend/settings.gradle`, `backend/src/main/java/dev/fanmeet/FanmeetApplication.java`
- Create: `backend/src/main/resources/application.yml`, `application-local.yml`
- Create: `docker-compose.yml` (MySQL 8, 포트 3306, DB명 fanmeet)
- Create: `.gitignore`, `backend/src/main/java/dev/fanmeet/common/HealthController.java`

**Interfaces:**
- Produces: `GET /api/health` → `{"status":"ok"}`. 베이스 패키지 `dev.fanmeet`.

**Steps:**
- [x] Spring Initializr 구성으로 프로젝트 생성 (web, jpa, security, oauth2-client, validation, lombok, mysql)
- [x] docker-compose로 MySQL 기동 (3306 점유 중이라 호스트 3307로), JPA 연결 확인
- [x] HealthController 작성, 401 관찰 완료
- [x] springdoc 3.0.3 (Boot 4 대응 버전) 추가
- [x] Commit 완료

### Task 2: User 엔티티 + SecurityConfig 뼈대

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/user/User.java`, `Role.java`, `UserRepository.java`
- Create: `backend/src/main/java/dev/fanmeet/config/SecurityConfig.java`
- Test: `backend/src/test/java/dev/fanmeet/user/UserRepositoryTest.java`

**Interfaces:**
- Produces: `User(id, provider, providerId, nickname, phone, phoneVerified, role)`,
  `Role { FAN, HOST }`, `UserRepository.findByProviderAndProviderId(String, String): Optional<User>`
- SecurityConfig: `/api/health`, `/swagger-ui/**`, `/v3/api-docs/**` permitAll, 나머지 authenticated

**Steps:**
- [x] `UNIQUE(provider, provider_id)` 제약 포함 엔티티 작성
- [x] `@DataJpaTest` 3케이스 red→green (Boot 4에서 DataJpaTest 패키지 변경됨: org.springframework.boot.data.jpa.test.autoconfigure)
- [x] SecurityConfig 작성, 401→200 확인 (미인증 시 403인 상태 — Task 5에서 401 entry point로 교정 예정)
- [x] Commit 완료

### Task 3: JwtProvider (TDD)

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/auth/JwtProvider.java`
- Test: `backend/src/test/java/dev/fanmeet/auth/JwtProviderTest.java`

**Interfaces:**
- Produces: `createAccessToken(Long userId, Role role): String` (만료 30분),
  `createRefreshToken(Long userId): String` (만료 14일),
  `parseUserId(String token): Long`, `parseRole(String token): Role`
- 시크릿: 환경변수 `JWT_SECRET`, HS256

**Steps:**
- [x] 테스트 먼저: 발급한 토큰에서 userId/role 회수, 만료 토큰 예외, 위조 서명 예외 (3케이스)
- [x] 실패 확인(사용자가 IntelliJ에서) → jjwt 0.13.0으로 구현 → 통과
- [x] Commit 완료

### Task 4: 카카오 OAuth2 로그인 → JWT 발급

**사전조건:** 준비물 A.

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/auth/CustomOAuth2UserService.java`, `OAuth2SuccessHandler.java`, `OAuthAttributes.java`
- Modify: `SecurityConfig.java`, `application-local.yml` (kakao provider/registration)
- Create: `frontend/` Vite React 최소 앱 — `src/pages/OAuthCallback.tsx`, `src/App.tsx`

**Interfaces:**
- Produces: 로그인 성공 시 `http://localhost:5173/oauth/callback#accessToken=...`로 리다이렉트,
  refresh 토큰은 HttpOnly 쿠키 `refresh_token`.
- `CustomOAuth2UserService`: 카카오 응답 → User upsert (없으면 FAN으로 가입)

**Steps:**
- [x] 카카오 registration/provider yml 설정 (+ application-secret.yml 분리, test 프로파일 더미 키)
- [x] CustomOAuth2UserService — 파싱(OAuthAttributes)·upsert 분리, 단위 테스트 4개
- [x] OAuth2SuccessHandler — access는 프래그먼트, refresh는 HttpOnly 쿠키(Path=/api/auth)
- [x] Vite(react-ts) 앱 + OAuthCallback 페이지, 토큰은 메모리 보관
- [x] 전체 플로우 수동 확인 (DB 유저 생성·쿠키 전달 확인. 미인증 API가 401 대신 카카오
  리다이렉트로 가는 현재 동작은 Task 5에서 entry point로 교정)
- [x] Commit 완료

### Task 5: JWT 인증 필터 + /api/me + 재발급/로그아웃

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/auth/JwtAuthenticationFilter.java`, `AuthController.java`, `RefreshToken.java`, `RefreshTokenRepository.java`
- Modify: `SecurityConfig.java`
- Test: `backend/src/test/java/dev/fanmeet/auth/AuthControllerTest.java` (MockMvc)

**Interfaces:**
- Produces: `Authorization: Bearer <access>` 인증. `GET /api/me` → `{id, nickname, role, phoneVerified}`.
  `POST /api/auth/refresh` (쿠키 기반, rotation — 기존 토큰 폐기 후 재발급),
  `POST /api/auth/logout` (DB에서 refresh 삭제 + 쿠키 만료)

**Steps:**
- [ ] MockMvc 테스트 먼저: 토큰 없이 /api/me → 401, 유효 토큰 → 200, 만료 → 401,
  refresh rotation(재사용된 구 토큰 거부), logout 후 refresh 거부 (5케이스)
- [ ] 필터·컨트롤러 구현 → 통과. 프론트에 axios 인터셉터(401 시 refresh 재시도) 추가
- [ ] Commit: `feat: JWT 인증 필터, 토큰 재발급/로그아웃`

### Task 6: 구글 로그인 추가

**사전조건:** 준비물 B.

**Files:**
- Modify: `application-local.yml`, `OAuthAttributes.java`

**Steps:**
- [ ] 구글 registration 추가, OAuthAttributes에 구글 파싱 분기 (+단위 테스트 1케이스)
- [ ] 수동 확인 후 Commit: `feat: 구글 로그인 추가`

### Task 7: SmsSender 인터페이스 + 구현체

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/sms/SmsSender.java`, `CoolSmsSender.java`, `ConsoleSmsSender.java`

**Interfaces:**
- Produces: `SmsSender.send(String phone, String message): void` (실패 시 `SmsSendException`)
- `ConsoleSmsSender`: `@Profile("local")` 기본 — 로그로만 출력(개발 중 과금 방지).
  실발송 확인은 `sms` 프로파일로 전환해 수행.

**Steps:**
- [ ] 인터페이스 + Console 구현 → CoolSMS 구현 (Nurigo SDK, 환경변수 API 키)
- [ ] `sms` 프로파일로 본인 번호에 실발송 1회 확인 (준비물 C)
- [ ] Commit: `feat: SmsSender 추상화 및 CoolSMS/콘솔 구현체`

### Task 8: 휴대폰 인증 (발송/검증)

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/sms/PhoneVerification.java`, `PhoneVerificationRepository.java`, `PhoneVerificationService.java`, `PhoneController.java`
- Test: `backend/src/test/java/dev/fanmeet/sms/PhoneVerificationServiceTest.java`

**Interfaces:**
- Produces: `POST /api/phone/request {phone}`, `POST /api/phone/verify {phone, code}` →
  성공 시 User.phone/phoneVerified 갱신.
- 규칙: 코드 6자리·5분 만료, 재발송 60초 쿨다운, 검증 5회 실패 시 코드 무효화.

**Steps:**
- [ ] 서비스 단위 테스트 먼저 (mock SmsSender): 정상 검증, 만료, 오답 5회 무효화, 쿨다운 (4케이스)
- [ ] 실패 확인 → 구현 → 통과 → 컨트롤러 + 프론트 번호 등록 폼
- [ ] Commit: `feat: SMS 휴대폰 인증 (만료/쿨다운/시도제한)`

### Task 9: Event/TimeSlot CRUD + HOST 권한

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/event/Event.java`, `TimeSlot.java`, `EventRepository.java`, `TimeSlotRepository.java`, `EventService.java`, `EventController.java`
- Test: `backend/src/test/java/dev/fanmeet/event/EventServiceTest.java`

**Interfaces:**
- Produces: `POST /api/events` (HOST만, `@PreAuthorize("hasRole('HOST')")`),
  `GET /api/events`, `GET /api/events/{id}` (슬롯 목록 + 슬롯별 잔여석 포함),
  `POST /api/events/{id}/close`. Event(deposit_amount, status[OPEN/CLOSED]),
  TimeSlot(startsAt, endsAt, capacity — 카운터 컬럼 없음).
- 잔여석 계산: `TimeSlotRepository.countActiveReservations(Long slotId): long`
  (활성 = PENDING_PAYMENT or CONFIRMED) — Task 10이 이 쿼리를 재사용.

**Steps:**
- [ ] 테스트 먼저: FAN이 개설 시도 → 403, HOST 개설 성공, CLOSED 이벤트 상세의 상태 노출 (3케이스)
- [ ] 구현 → 통과. HOST 승격은 v1에선 SQL로 수동 처리(문서화)
- [ ] 프론트: 이벤트 목록/상세 페이지 (잔여석 표시)
- [ ] Commit: `feat: 이벤트/슬롯 CRUD와 HOST 권한`

### Task 10: 예약 신청 (락 + 정원/중복 검증)

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/reservation/Reservation.java`, `ReservationStatus.java`, `ReservationRepository.java`, `ReservationService.java`, `ReservationController.java`
- Test: `backend/src/test/java/dev/fanmeet/reservation/ReservationServiceTest.java`, `ReservationConcurrencyTest.java`

**Interfaces:**
- Produces: `POST /api/reservations {slotId}` → `{reservationId, amount, orderId}` (PENDING_PAYMENT).
  `ReservationService.reserve(Long userId, Long slotId): ReserveResult(reservationId, amount, orderId)`.
  검증 순서: phoneVerified → 슬롯 `@Lock(PESSIMISTIC_WRITE)` 획득 → 정원 COUNT → 활성 중복 예약 검사.
  orderId 형식: `fanmeet-{reservationId}` — Task 11 결제 검증이 이 형식에 의존.

**Steps:**
- [ ] 단위 테스트 먼저: 정원 초과 거부, 중복 예약 거부, 미인증 휴대폰 거부, 취소 후 재예약 허용 (4케이스)
- [ ] 통합 동시성 테스트: 스레드 10개가 정원 3 슬롯에 동시 예약 → 성공 정확히 3 (MySQL 필요, `@SpringBootTest`)
  — 설명 포인트: 락 없이 돌려 실패를 먼저 관찰(v2 동시성 서사의 씨앗)
- [ ] 구현 → 통과 → 프론트 예약 버튼 연결
- [ ] Commit: `feat: 슬롯 예약 신청 (비관적 락 정원 검증)`

### Task 11: PaymentGateway + 결제 검증·확정

**사전조건:** 준비물 D.

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/payment/PaymentGateway.java`, `PortOnePaymentGateway.java`, `Payment.java`, `PaymentRepository.java`, `PaymentService.java`, `PaymentController.java`
- Test: `backend/src/test/java/dev/fanmeet/payment/PaymentServiceTest.java`

**Interfaces:**
- Produces: `PaymentGateway.getPayment(String paymentId): GatewayPayment(paymentId, orderId, amount, status)`,
  `PaymentGateway.cancel(String paymentId, String reason): void`.
  `POST /api/payments/confirm {paymentId}` → 조회·금액 대조 → 일치: Payment(PAID) + CONFIRMED + 확정 SMS /
  불일치: `cancel()` 즉시 환불 + Payment(REFUNDED), 예약 PENDING 유지.
  멱등: 동일 paymentId로 이미 PAID면 200 no-op.

**Steps:**
- [ ] 단위 테스트 먼저 (mock PaymentGateway/SmsSender): 금액 일치 확정, 불일치 자동환불,
  중복 confirm 멱등, 만료된 예약에 결제 → 환불, SMS 실패해도 확정 유지 (5케이스)
- [ ] 구현 → 통과 → PortOne 구현체 작성
- [ ] Commit: `feat: 결제 검증·확정 (서버측 금액 대조, 멱등)`

### Task 12: 프론트 결제창 연동 + 웹훅

**Files:**
- Modify: `frontend/` — PortOne 브라우저 SDK로 결제창 호출, 완료 시 `/api/payments/confirm`
- Create: `backend/src/main/java/dev/fanmeet/payment/PortOneWebhookController.java`

**Interfaces:**
- Produces: `POST /api/webhooks/portone` — 서명 검증 후 PaymentService.confirm 재사용 (permitAll + 서명검증)

**Steps:**
- [ ] 프론트 결제 플로우 연결, PortOne 테스트 결제로 E2E 수동 확인 (결제→확정→콘솔 SMS 로그)
- [ ] 웹훅 컨트롤러 (서명 검증 포함), 터널로 실제 웹훅 수신 확인 — 설명 포인트: 콜백 유실 시나리오
- [ ] Commit: `feat: 결제창 연동 및 PortOne 웹훅`

### Task 13: 만료 스케줄러

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/reservation/ReservationExpirer.java`
- Test: `ReservationExpirerTest.java`

**Steps:**
- [ ] 테스트 먼저: 30분 경과 PENDING → EXPIRED, 29분은 유지 (2케이스, Clock 주입으로 시간 제어)
- [ ] `@Scheduled(fixedDelay=60_000)` 구현 → 통과
- [ ] Commit: `feat: 미결제 예약 만료 스케줄러`

### Task 14: 취소/환불 + 마이페이지

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/reservation/` 취소 로직 (ReservationService에 추가)
- Modify: `ReservationController.java`, `PaymentService.java`, 프론트 마이페이지
- Test: `ReservationServiceTest.java`에 추가

**Interfaces:**
- Produces: `DELETE /api/reservations/{id}` — 본인+CONFIRMED만, 슬롯 시작 후 불가.
  환불 성공: Payment(REFUNDED)+CANCELLED+취소 SMS / 환불 실패: Payment(REFUND_FAILED), 예약 CONFIRMED 유지.
  `GET /api/me/reservations` — 내 예약 목록(상태 포함).

**Steps:**
- [ ] 테스트 먼저: 타인 예약 취소 → 403, 시작 후 취소 거부, 환불 실패 시 상태 보존 (3케이스)
- [ ] 구현 → 통과 → 프론트 마이페이지(목록+취소 버튼)
- [ ] Commit: `feat: 예약 취소/환불과 마이페이지`

### Task 15: 마무리 — 에러 응답 통일 + README

**Files:**
- Create: `backend/src/main/java/dev/fanmeet/common/GlobalExceptionHandler.java`, `ErrorResponse.java`, `README.md`

**Steps:**
- [ ] `@RestControllerAdvice` — 도메인 예외를 `{code, message}` 포맷으로 통일, 기존 테스트의 에러 응답 단언 보강
- [ ] README: 실행 방법, 환경변수 목록, 아키텍처 요약, v2 로드맵
- [ ] 전체 테스트 `./gradlew test` 그린 확인
- [ ] Commit: `chore: 전역 에러 처리와 README`

---

## Self-Review 결과

- 스펙 커버리지: 스펙 §2 표의 8개 항목 모두 태스크에 매핑됨 (OAuth2→4·6, JWT→3·5,
  휴대폰 인증→7·8, CRUD→9, 예약+결제→10·11·12, 환불→14, SMS 알림→7·11·14, Swagger→1).
  만료 스케줄러(§8-6)→13, 에러 처리(§11)→15.
- 타입 일관성: `SmsSender.send(phone, message)`, `PaymentGateway.getPayment/cancel`,
  `orderId = fanmeet-{reservationId}`, `countActiveReservations` — 태스크 간 참조 일치 확인.
- 코드 전문 미포함은 사용자 합의에 따른 의도적 결정(헤더 참조).
