# 이 파일을 복사해서 같은 위치에 application-secret.yml 로 저장하고 실제 값을 채운다.
# application-secret.yml 은 .gitignore 에 등록되어 있어 커밋되지 않는다.
spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 카카오_REST_API_키
            client-secret: 카카오_Client_Secret
