spring:
  datasource:
    url: jdbc:mysql://localhost:3307/fanmeet
    username: root
    password: localdev
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
